# Toolchain, pinned so the CI gates depend on the code rather than on when
# they happen to run. Previously CI did `pip install ruff mypy` unpinned.
-r requirements.txt

ruff==0.16.5
mypy==1.11.2
pytest==8.3.2
