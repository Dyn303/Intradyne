ccxt==4.3.77
aiohttp==3.14.3
websockets==12.0
fastapi==0.114.2
uvicorn==0.30.6
pydantic==2.9.2
python-dotenv==1.2.2
numpy==2.1.1
pandas==2.2.3
loguru==0.7.2
orjson==3.11.6
httpx==0.27.2
prometheus-client==0.20.0
optuna==3.6.1
matplotlib==3.9.2
pydantic-settings==2.6.1
# Only imported when DB_URL names a Postgres database; the SQLite default
# needs nothing beyond the standard library. [binary] ships the libpq wheel so
# the slim image needs no build toolchain, [pool] is the connection pool that
# keeps three stores from opening a TCP connection per query.
psycopg[binary,pool]==3.3.5
